你们用java试试看。 



List<Mapper>   getMappers(List<Inteter> pay, List<integer> payback) {

  void loop(List<Integer>  a,  List<integer>[] b,  List<Mapper>  accumulator) {
      // todo
      // 不断累积结果到accucumulator上。 
      // 不断取首部元素， 来抵消， 掉为0后， 移动到下一个元素。 
  }


  
  List<Mapper> acc =  new List<>();
  loop(pay, payback, acc);
  return acc; 
}